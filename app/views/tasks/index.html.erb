<h1>Tasks List</h1>
<h4>
  <%= "These are all tasks:" %>
</h4>
<p>
  <ul>
    <% @tasks.each do |task| %>
    <ul
      <li>
      <%= link_to task.name.capitalize, task_path(task.id) %>

      </li>
    </ul>

  <% end %>
  <p>
    <%= link_to "Add a new task", "tasks/new" %>
  </p>
</p>
