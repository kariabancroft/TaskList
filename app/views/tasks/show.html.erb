<h1><%= @result_task.name.capitalize %>:</h1>

  <h4>
    <%= @result_task.description.capitalize %>
  </h4>
  <p>
    Task completed:
    <% if @result_task.completion_date == nil %>
      <%= "☹️ Not yet" %>
      <% else %>
    <%=  "🌟 #{@result_task.completion_date}" %>
    <% end %>
  </p>
  </ul>
</p>
<p>

  <%= button_to "Back to List", tasks_path, method: :get %>
</p>
